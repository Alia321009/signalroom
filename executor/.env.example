# Copy to executor/.env (or run from wherever this file lives) and fill in.
# This runs on a MEMBER's own machine, not the server.

API_BASE_URL=https://your-signalroom-domain.example
API_KEY=
POLL_INTERVAL_SECONDS=5.0
STATE_FILE=./executor_state.json
